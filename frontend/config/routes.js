const routes = {
    students: '/students',
    programs: '/programs',
    faculties: '/faculties',
    statuses: '/statuses',
    logs: '/logs',
    studentsImport: '/students/import',
    settings: '/settings',
};

export default routes;
