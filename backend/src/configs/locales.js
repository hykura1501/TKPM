// Danh sách các ngôn ngữ được hỗ trợ
const SUPPORTED_LOCALES = ["en", "vi"]; // English, Vietnamese, Japanese

// Nhãn hiển thị cho từng ngôn ngữ
const LOCALE_LABELS = {
  en: "English",
  vi: "Tiếng Việt",
};

module.exports = {
  SUPPORTED_LOCALES,
  LOCALE_LABELS,
};